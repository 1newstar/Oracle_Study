--写一个匿名块，在system(system/manager)用户下执行。
--新建一个directory : HAND_DIR_TEMP , 物理地址为虚拟机的 c:\temp 目录, 
--并授权 读写 权限给student1用户，然后将hand_exam_data.txt这个文件放到c:\temp 目录下。（5分)
--this is system
DECLARE

BEGIN

END;
/
